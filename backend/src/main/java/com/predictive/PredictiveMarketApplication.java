package com.predictive;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * PredictiveMarketApplication — Spring Boot entry point.
 * AI Business & Financial Dashboard backend.
 */
@SpringBootApplication
public class PredictiveMarketApplication {
    public static void main(String[] args) {
        SpringApplication.run(PredictiveMarketApplication.class, args);
    }
}
